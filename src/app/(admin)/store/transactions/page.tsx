// PURPOSE: Store-owner transactions view — the owner's own store, filterable.
// TODO: Replace dummy data with useTransactions() when the API is ready.

import { TransactionsTable } from "@/components/admin/TransactionsTable";

export default function StoreTransactionsPage() {
  return <TransactionsTable />;
}
