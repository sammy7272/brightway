// PURPOSE: Super-admin store management — list, activate/deactivate, view stores.
// TODO: Wire "Add store" + real data when the API is ready.

import { StoresTable } from "@/components/super-admin/StoresTable";

export default function StoresPage() {
  return <StoresTable />;
}
