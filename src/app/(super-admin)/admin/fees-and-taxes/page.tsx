// PURPOSE: Super-admin Fees & Taxes configuration (global tax + per-store rates).
// TODO: Persist changes via the API when the backend is ready.

import { FeesForm } from "@/components/super-admin/FeesForm";

export default function FeesAndTaxesPage() {
  return <FeesForm />;
}
